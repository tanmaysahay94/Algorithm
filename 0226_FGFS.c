#include<stdio.h>
typedef struct guest
{
	unsigned long long int s,f,p,checkIn;
}guest;
int main()
{
	unsigned long long int t,n,k;
	scanf("%llu",&t);
	while(t--)
	{
		scanf("%llu%llu",&n,&k);
		guest g[n];
	}
	return 0;
}

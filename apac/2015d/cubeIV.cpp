#include <bits/stdc++.h>

using namespace std;

const int maxs = 1e3 + 3;

vector<vector<int> > mat(maxs, vector<int> (maxs));
int S;

int main()
{
	int t;
	scanf("%d", &t);
	while (t--)
	{
		scanf("%d", &S);
		for (int i = 0; i < S; i++)
	}
	return 0;
}

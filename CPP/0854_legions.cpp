#include <bits/stdc++.h>

using namespace std;

void optimizeIO()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
}

typedef long long LL;

const LL mod = 1e8;

LL n1, n2, k1, k2;
LL n;
LL dp[205][2][15];

LL solve(LL idx, LL a, LL b)
{
}

int main()
{
	optimizeIO();
	memset(dp, -1, sizeof(dp));
	cin >> n1 >> n2 >> k1 >> k2;
	n = n1 + n2;
	return 0;
}
